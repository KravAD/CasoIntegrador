#include <iostream>
#include <vector>
#include <string>
#include <map>
#include "json11.hpp"
#include "jsonlib.hpp"

enum variant_type { simbolo, numero, lista, proc, lambda, cadena};

struct entorno;

class variant {
    
};